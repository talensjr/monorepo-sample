{
  "name": "monorepo-ui",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "lerna run start --parallel",
    "build": "yarn workspace @monorepo-ui/app build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.15.0"
  }
}
